#!/bin/sh
# Prevent Cursor Agent from appearing as a GitHub contributor.
ROOT="$(git rev-parse --show-toplevel)"
python "$ROOT/.githooks/git_msg_strip_cursor.py" < "$1" > "$1.tmp" && mv "$1.tmp" "$1"
